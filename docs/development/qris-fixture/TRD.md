# TRD (Hub): QRIS Fixture (liveness fixture for feature-detail J4)

A synthetic SDLC document tree. It exists so the `feature-detail` liveness journey can change a
document, refresh, and watch the derived phase and the evidence move. It describes no real product.

## 1. Context / scope
_Approved: 2026-08-19_

One feature, four numbered sections, two of them approved — so the screen should read
`section 2 of 4` on first import.

## 2. Feature dependencies
_Approved: 2026-08-19_

None. The fixture depends on nothing.

## 3. System design

Deliberately unapproved, so the count is a fraction rather than a completion.

## 4. Change manifest

Deliberately unapproved.
